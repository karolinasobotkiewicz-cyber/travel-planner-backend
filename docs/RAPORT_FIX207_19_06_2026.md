# RAPORT TECHNICZNY — FIX #207 (19.06.2026)

**Zakres:** feedback klientki dla **Wrocław** i **Poznań** (po FIX #206).

---

## 1. Dane — Ogród Botaniczny Wrocławski (Wrocław #1)

**Przyczyna:** w Excelu `City=Kraków`, współrzędne Krakowa (`50.063, 19.955`).

**Naprawa (`multi_city_attractions.xlsx`):**
- `City` / `Hub` → Wrocław
- `Lat/Lng` → `51.108056, 17.073056`

## 2. Local Food — brak Hali Targowej (Wrocław #9)

**Przyczyna:** import deduplikował po samym `Name` — „Hala Targowa" z Gdańska
blokowała wpis dla Wrocławia.

**Naprawa:** dodano **Hala Targowa (Wrocław)** z planer8 (klucz Name+City).

**Uwaga:** **Browar Stu Mostów** nie występuje w żadnym arkuszu klientki —
wymaga uzupełnienia danych po stronie klientki.

## 3. Relaxation — fałszywe pokrycie (Wrocław #8)

**Przyczyna:** keyword `spa` trafiał jako substring w `event_space` (Hala Stulecia)
i `exhibition_space` (Muzeum Narodowe).

**Naprawa (`preference_coverage.py`):**
- `_kw_matches_tag()` — krótkie słowa (≤3 znaki) muszą pasować do całego tokenu tagu.
- `_COVERAGE_NAME_DENY` dla relaxation: hala stulecia, muzeum narodowe, pergola, hydropolis.

## 4. Mikro-POI jako core (Poznań #2, Wrocław #6)

**Naprawa (`engine.py`):**
- Rozszerzone `_HARD_QUICK_STOP_MARKERS` / `_QUICK_STOP_NAME_MARKERS`:
  pręgierz, domy kupieckie, okrąglak, plac wolności, pomnik ofiar/bamber,
  bastion, wyspa słodowa, most grunwaldzki (via `most `).

**Naprawa danych (Poznań):** democja `priority_level` 12/core→2 i `must_see` 8→5
dla: Pręgierz, Domy Kupieckie, Okrąglak, Plac Wolności, Pomnik Ofiar Czerwca 1956.

## 5. Nature Landscape słabo wybierane (Wrocław #2, Poznań #4)

**Naprawa (`engine.py`, tylko city_tourism):**
- **NATURE BOOST +45** gdy `nature_landscape` w top-3 preferencjach.
- **-35** na muzea gdy `nature_landscape` + `museum_heritage` oba w top-3
  (żeby muzea nie wypierały natury w planach mieszanych).

## 6. Adventure — Rynek, Ostrów Tumski… (Wrocław #4)

**Naprawa (`engine.py`, city_tourism + adventure):**
- **-45** na iconic heritage fillers (rynek, ostrów tumski, stare miasto, ratusz)
  gdy brak `museum_heritage` / `history_mystery` w top-3.
- Mikro-POI (bastion, most, wyspa) → quick-stop (FIX #206 + #207 markery).

## 7. Local Food Experience słabo (Wrocław #9)

**Naprawa (`engine.py`, city_tourism):**
- **LOCAL FOOD BOOST +40** gdy `local_food_experience` w top-3
  (hala targowa, browar, food_hall, craft_beer…).

## 8. Free Time (Wrocław #3, Poznań #1/#5)

Bez nowej logiki w tej rundzie — korzysta z FIX #206 (cap 45 min post-dinner)
i FIX #195 (afternoon top-up). Długie bloki w planach 5–7 dni wymagają
osobnej iteracji (backfill density).

## 9. Friends profil słaby (Wrocław #5)

Bez zmian architektonicznych w tej rundzie — FIX #206 friends boost pozostaje.
Pełna separacja profilu Friends wymaga osobnego tasku (jak Kraków/Warszawa).

---

## Testy (PASS)

| Test | Wynik |
|------|-------|
| `test_fix203.py` | 45 coverage + micro-POI PASS |
| `test_naturalness_klientka.py` | **10/10** Zakopane |
| Regresja Zakopane | brak (city_tourism gate na boostach #207) |

---

## Pliki zmienione

- `app/domain/scoring/preference_coverage.py`
- `app/domain/planner/engine.py`
- `data/multi_city_attractions.xlsx`
- `test_fix203.py`
