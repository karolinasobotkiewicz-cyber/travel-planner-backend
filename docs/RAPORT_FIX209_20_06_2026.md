# RAPORT TECHNICZNY — FIX #209 (20.06.2026)

**Zakres:** Kudowa-Zdrój, Polanica-Zdrój, Kłodzko (Kotlina Kłodzka).

---

## 1. Puste dni przy `family_kids` (#2 Polanica, #2 Kudowa)

**Przyczyna (warstwa 1):** `should_exclude_by_target_group` — tylko 2 POI miały
`family_kids` w Excelu (Park Wodny, Góralka); reszta była odrzucana.

**Przyczyna (warstwa 2):** Po naprawie sezonowości zimowy pool Polanicy spadł
z ~21 do ~10 POI — strefy A/B/C wyczerpywały pulę do dnia 3.

**Naprawa:**
- `family_fit.py`: `family_kids` akceptuje POI z `friends` / `couples` / ogólnych
  grup (nie `seniors`-only).
- `plan_service.py`: **Kotlina soft-cluster** — gdy hub ma <35 POI, ładujemy
  pełny klaster (Kłodzko+Polanica+Kudowa) + `build_cluster_hub_day_pools`
  z `base_city=requested_city`.

## 2. Góralka w lutym (#6 Polanica)

**Przyczyna:** `normalize_seasonality()` czytało `Seasonality of attractions`, ale
loader multi-city zapisywał tylko klucz `season` → `season_fit` = all-year (1,1,1,1).
Dodatkowo `fallback_pois` w `build_day` omijało filtr sezonowy (sparse-mode gap-fill).

**Naprawa:**
- `normalizer.py`: fallback `season` / `seasonality` przy budowie `season_fit`.
- `engine.py`: `filter_by_season(fallback_pois)` w `build_day`.
- `seasonality.py`: nazwowe odrzucenie zimą (`letni tor`, `góralka`…).
- `plan_service.py`: `_strip_out_of_season_attractions` jako safety net.

## 3. Local Food Experience (#3 Kudowa, #3 Polanica)

**Przyczyna:** POI kulinarne w **Kłodzko** (hub); tagi `cheese`, `fishery` poza
słownikiem scoringu.

**Naprawa:**
- `tag_preferences.py`: cheese, fishery, craft_food, regional_products.
- `engine.py`: LOCAL FOOD BOOST +40 (sery, pstrąg, manufaktura…).
- Kotlina soft-cluster umożliwia trafienie w Sery Lutomierskie / Złoty Pstrąg.

## 4. Relaxation → free_time (#5)

**Naprawa:**
- `city_copy.multi_city_density_mode`: `regional_cluster` + pool <30.
- `engine.py`: SPA RELAX BOOST +50 (pijalnia, park zdrojowy, park wodny).
- `plan_service.py`: sygnał `cluster_type=regional_cluster` dla miast Kotliny.

## 5. Mikro-POI (#6)

**Naprawa:** quick-stop markery: Brama Wodna, Plac Chrobrego, Kamienny Most,
Most św. Jana (już przez `most ` w `_HARD_QUICK_STOP_MARKERS`).

## 6. Kopalnia Złota → nature_landscape (#2 Kudowa)

**Naprawa (`preference_coverage.py`):** name-deny + tagi kopalni w
`_NATURE_COVERAGE_TAG_DENY`.

## 7. Powtórki museum+history (#4 Kudowa)

**Naprawa (`engine.py`):** FIX #173 — kara -70 po 3. muzeum; redukcja boostu
w małych poolach (<80 POI).

## 8. Free Time (#1, #3)

Częściowo — gęstsze plany (6 atrakcji/dzień), soft-cluster; free_time nadal
występuje (135–171 min/dzień Polanica test-01). Pełny backfill — osobny task.

---

## Testy (PASS)

| Test | Wynik |
|------|-------|
| `test_fix203.py` | **54/54** PASS |
| `test_naturalness_klientka.py` | **10/10** PASS |
| Polanica test-01 (family_kids, 2026-02-20) | 6/6/6 atrakcji, **brak Góralki** |

---

## Pliki

- `app/infrastructure/repositories/normalizer.py`
- `app/domain/filters/seasonality.py`
- `app/domain/scoring/family_fit.py`
- `app/domain/scoring/preference_coverage.py`
- `app/domain/scoring/tag_preferences.py`
- `app/domain/planner/city_copy.py`
- `app/domain/planner/engine.py`
- `app/application/services/plan_service.py`
- `test_fix203.py`
